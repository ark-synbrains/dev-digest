# Graphify install / Cursor rules — never index tooling into the project graph
.agents/skills/graphify/
.cursor/rules/
# Tracked in git, but excluded from the *codebase* knowledge graph:
agent/out/
agent/digest-corpus/
agent/digest-graph/
digests/
